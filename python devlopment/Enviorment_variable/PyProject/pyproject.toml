[project]
name = "goutam"
version = "0.1.0"
description = "pyProjeect poetry-example"
authors = [
    {name = "goutam",email = "goutam.kushwah2003@gmail.com"}
]
requires-python = ">=3.11"
dependencies = [
    "requests (>=2.33.1,<3.0.0)",
    "pendulum (>=3.2.0,<4.0.0)",
    "panda (>=0.3.1,<0.4.0)",
    "pypi (>=2.1,<3.0)",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
